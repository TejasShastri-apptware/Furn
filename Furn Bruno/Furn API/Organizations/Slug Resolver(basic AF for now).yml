info:
  name: Slug Resolver(basic AF for now)
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/api/orgs/resolve/testorg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
