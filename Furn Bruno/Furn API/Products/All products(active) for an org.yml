info:
  name: All products(active) for an org
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5000/api/products
  headers:
    - name: x-org-id
      value: "5"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
